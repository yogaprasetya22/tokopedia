{
  "version": 2,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ],
  "build": {
    "commands": {
      "install": "pnpm add -D @prisma/client",
      "build": "npx prisma generate && next build"
    }
  }
}